// fix scroll value based on picked boilerplate

// export const getBoundingClientRect = (element, scrolling) => {
//   const bounds = element.getBoundingClientRect();

//   let scroll = 0;

//   if (!scrolling) {
//     scroll = window.App?.scrolling?.current || window.pageYOffset;
//   }

//   return {
//     bottom: bounds.bottom + scroll,
//     height: bounds.height,
//     left: bounds.left,
//     right: bounds.right,
//     top: bounds.top + scroll,
//     width: bounds.width,
//   };
// };
